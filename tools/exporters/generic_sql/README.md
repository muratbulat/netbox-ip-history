# Generic SQL export

Use a read-only database account and export a view with stable column names. Configure the column mapping in `ImportSource`; unrestricted SQL is intentionally not accepted by the plugin.