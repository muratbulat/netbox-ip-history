# Microsoft IPAM export

Export IPAM data on the Windows IPAM server with an administrator-reviewed PowerShell script and transfer CSV or JSON to NetBox. The NetBox plugin does not require PowerShell on the NetBox host. Do not embed credentials in export scripts.